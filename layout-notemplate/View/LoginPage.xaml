<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShop.View.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="Username" Margin="5"/>
            <TextBox x:Name="usernameTextBox" Margin="5" Text="{Binding User, Mode=TwoWay, UpdateSourceTrigger =PropertyChanged}"/>

            <TextBlock Text="Password" Margin="5" />
            <PasswordBox x:Name="passwordBox" Margin="5" Password="{Binding Pass, Mode=TwoWay, UpdateSourceTrigger= PropertyChanged}"/>

        <Button Content="Login" Margin="5" HorizontalAlignment="Center" Command="{Binding Login}"/>
        <TextBlock Text="{Binding Mess, Mode=TwoWay}" Foreground="Red" FontSize="15" x:Name="messageTextBlock"/>
    </StackPanel>

</Page>
