<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShop.View.ProductsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >

    <Grid>
        <Grid Width="900">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
            </Grid.RowDefinitions>
            
            <!--Header & Add, Edit Import button-->
            <Grid Grid.Row="0">
                <Grid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="Black"></Setter>
                        <Setter Property="FontWeight" Value="SemiBold"></Setter>
                        <Setter Property="FontSize" Value="20"></Setter>
                    </Style>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Products"/>
                <Button x:ConnectionId='6' Grid.Column="1" Background="White" Margin="0 0 10 0"                        >Add</Button>
                <Button Grid.Column="2" Background="White">Import</Button>
            </Grid>
   
            <!--Search bar-->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:ConnectionId='5' Width="200" Name="productNameTextBox" TextWrapping="Wrap" Height="30" Background="White" PlaceholderText="Product Name" Text="{Binding SearchName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"></TextBox>
                        <Button Margin="10 0" Command="{Binding Filter}">Search</Button>
                        
                    </StackPanel>
                </Grid>
            </Grid>
            
            <!--Filter, Sort-->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="240"/>
                    <ColumnDefinition Width="260"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" Margin="0 9 0 0">
                    <ComboBox PlaceholderText="Pick a Price range" Width="200"
                               SelectedItem="{Binding Option1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              ItemsSource="{Binding SelectOption1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              >
                       
                    </ComboBox>
                </Grid>
                <Grid Grid.Column="1" Margin="0 9 0 0">
                    <ComboBox PlaceholderText="Pick a Manufacturer" Width="200"
                               DisplayMemberPath="tenloai"
                               SelectedValuePath="tenloai"
                               SelectedItem="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                               ItemsSource="{Binding AllType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    >
                    </ComboBox>
                </Grid>
                <Grid Grid.Column="2">
                    <StackPanel Orientation="Horizontal">
                        <Button Command="{Binding Filter, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">Filter</Button>
                        <Button Command="{Binding ClearFilter, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">Remove Filter</Button>
                    </StackPanel>
                </Grid>
                <Grid Grid.Column="3" Margin="0 9 0 0">
                    <ComboBox PlaceholderText="Sort by" Width="200"
                               SelectedItem="{Binding Option2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                               ItemsSource="{Binding SelectOption2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              >
                    </ComboBox>
                </Grid>
            </Grid>
            
            <!--Data-->
            <Grid Grid.Row="3">
                <GridView x:ConnectionId='2' x:Name="ContentGridView" ItemsSource="{Binding Products}">
                    <GridView.ItemContainerStyle>
                        <Style TargetType="GridViewItem">
                            <Setter Property="Margin" Value="5, 5, 5, 5"/>
                        </Style>
                    </GridView.ItemContainerStyle>
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid x:Name="MaxItemsWrapGrid" MaximumRowsOrColumns="5" Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="30 5 30 5" Background="White" Padding="20" CornerRadius="8">
                                <Image Source="{Binding anh}" Width="50"/>
                                <StackPanel Margin="0,10,0,0">
                                    <TextBlock TextAlignment="Center" Text="{Binding tensp}" FontWeight="Bold"></TextBlock>
                                    <TextBlock TextAlignment="Center" Text="{Binding gia}" FontStyle="Italic"></TextBlock>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </Grid>
            
            <!--Page navigation-->
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                    <TextBlock Margin="0 5 0 0" TextAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold">Product per page</TextBlock>
                    <TextBox Width="10" Height="30" Text="{Binding PerPage, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" TextAlignment="Center" Margin="10 0"></TextBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="10,0,0,0">
                    <Button Width="80" Margin="10 0" Command="{Binding Prev}">Previous</Button>
                    <TextBlock Height="30" Text="{Binding Page2k, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" TextAlignment="Center" VerticalAlignment="Center"></TextBlock>
                    <Button Width="80" Margin="10 0" Command="{Binding Next}">Next</Button>
                </StackPanel>

            </Grid>
        </Grid>
    </Grid>
</Page>

