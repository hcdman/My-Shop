<?xml version="1.0" encoding="utf-8"?>
<Page x:ConnectionId='1'
    x:Class="MyShop.View.detailOrderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
     Background="{ThemeResource MyBackgroundThemeBrush}"
     Foreground="{ThemeResource MyForegroundThemeBrush}"
                     
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    >

    <Grid Margin="{StaticResource PageContentMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition ></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock FontSize="15" FontWeight="Bold" Margin="10">Detail order</TextBlock>
        <TextBlock Text="{Binding mess}">Demo</TextBlock>
        <ProgressBar x:ConnectionId='2' x:Name="loading" Width="500" Background="{ThemeResource MyBackgroundThemeBrush}"  IsIndeterminate="{Binding isShowProgressBar}" Height="10" Canvas.Left="94" Canvas.Top="313" HorizontalAlignment="Center" VerticalAlignment="Top"/>
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid Height="40">
                    <TextBlock Margin="8"  IsHitTestVisible="False"  Text="Search in Customers ..." Visibility="Visible"/>
                    <TextBox x:ConnectionId='8' Name="textBoxFilter" Text="{Binding keyword, Mode=TwoWay}" Height="20"/>
                </Grid>
                <Button Grid.Column="1" Margin="10,0,0,0" >Search</Button>
            </Grid>
            <Button x:ConnectionId='7' Grid.Column="1" Width="100" HorizontalAlignment="Right"                       >Add</Button>
        </Grid>

        <!--Data of customer-->
        <Grid Grid.Row="2" Background= "{ThemeResource MyBlockBackgroundThemeBrush}" Width="700" Height="350">
            <controls:DataGrid x:ConnectionId='3' x:Name="CustomerDataGrid" ItemsSource="{Binding detailOrderProduct, Mode=TwoWay}" AutoGenerateColumns="False" HorizontalAlignment="Center">

                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="ID" CanUserResize="False" Width="SizeToHeader" Binding="{Binding id_cthd}" FontWeight="Medium"/>
                    <controls:DataGridTextColumn Header="Order ID" CanUserResize="False" Width="SizeToHeader" Binding="{Binding sohd}" FontWeight="Medium"/>
                    <controls:DataGridTextColumn Header="Product ID" Binding="{Binding masp}" CanUserResize="False" Width="SizeToCells" FontWeight="Medium"/>
                    <controls:DataGridTextColumn Header="Quantity" Binding="{Binding sl}" CanUserResize="False"  Width="auto" FontWeight="Medium"/>
                    <controls:DataGridTextColumn Header="Product Image" Binding="{Binding anh}" CanUserResize="False"  Width="SizeToCells" FontWeight="Medium"/>
                    <controls:DataGridTextColumn Header="Name Product" Binding="{Binding tensp}" CanUserResize="False"  Width="SizeToCells" FontWeight="Medium"/>
                    <controls:DataGridTextColumn Header="Manufacturer" Binding="{Binding hangsx}" CanUserResize="False"  Width="SizeToCells" FontWeight="Medium"/>
                    <controls:DataGridTextColumn Header="Price" Binding="{Binding gia}" CanUserResize="False"  Width="SizeToCells" FontWeight="Medium"/>


                    <controls:DataGridTemplateColumn x:ConnectionId='4' x:Name="ope" Header="Operations" IsReadOnly="True" CanUserResize="False"  Width="SizeToHeader" >
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <StackPanel>
                                        <Button  Margin="5 0 0 0">
                                            <Image  Source="ms-appx:///Icons/edit.png"  Width="16"  />
                                        </Button>
                                    </StackPanel>
                                    <Button x:ConnectionId='6' Margin="5 0 0 0" Tag="{Binding id_cthd}"                          >
                                        <Image  Source="ms-appx:///Icons/delete.png" Width="16"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Grid>
        <!--Pagination-->
        <Grid Grid.Row="3" Width="700">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                <TextBlock FontWeight="Bold">Customer per page</TextBlock>
                <Border Background= "{ThemeResource MyBlockBackgroundThemeBrush}" Margin="5,0,0,0">
                    <TextBlock Text="{Binding _rowsPerPage}"></TextBlock>
                </Border>

            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="10,0,0,0">
                <Button>Previous</Button>
                <Border Margin="10"  Background= "{ThemeResource MyBlockBackgroundThemeBrush}">
                    <TextBlock>
                    <Run Text="{Binding _currentPage}"/>/<Run Text="{Binding _totalPages}"/>
                    </TextBlock>
                </Border>
                <Button>Next</Button>
            </StackPanel>

        </Grid>
    </Grid>
</Page>

