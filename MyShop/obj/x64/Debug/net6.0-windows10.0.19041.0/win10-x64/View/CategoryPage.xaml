<?xml version="1.0" encoding="utf-8"?>
<Page
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    x:Class="MyShop.View.CategoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource MyBackgroundThemeBrush}"
    Foreground="{ThemeResource MyForegroundThemeBrush}">

    <Grid Margin="{StaticResource PageContentMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition ></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock FontSize="15" FontWeight="Bold" Margin="10">Category</TextBlock>
        <TextBlock Text="{Binding mess}">Demo</TextBlock>
        <ProgressBar x:ConnectionId='2' x:Name="loading" Width="500" Background="{ThemeResource MyBackgroundThemeBrush}"  IsIndeterminate="{Binding isShowProgressBar}" Height="10" Canvas.Left="94" Canvas.Top="313" HorizontalAlignment="Center" VerticalAlignment="Top"/>
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <!--Search information-->
                <Grid Height="40">
                    <TextBlock Margin="8" IsHitTestVisible="False"  Text="Category Name" Visibility="Visible"/>
                    <TextBox x:ConnectionId='10' Name="textBoxFilter" Text="{Binding keyword, Mode=TwoWay}" Height="20"/>
                </Grid>
                <Button Grid.Column="1" Margin="10,0,0,0">Search</Button>
            </Grid>
            <Button x:ConnectionId='9' Grid.Column="1" Width="100" HorizontalAlignment="Right"                >Add</Button>
        </Grid>

        <!--Data of category-->
        <Grid Grid.Row="2" Background= "{ThemeResource MyBlockBackgroundThemeBrush}" Width="700" Height="350">
            <controls:DataGrid x:ConnectionId='5' x:Name="CustomerDataGrid" ItemsSource="{Binding categories}" AutoGenerateColumns="False" HorizontalAlignment="Center">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="Id" CanUserResize="False" Width="SizeToHeader" Binding="{Binding maloai}" FontWeight="Medium"/>
                    <controls:DataGridTextColumn Header="Name" Binding="{Binding tenloai}" CanUserResize="False" Width="SizeToHeader"  FontWeight="Medium"/>

                    <controls:DataGridTemplateColumn Header="Operations" IsReadOnly="True" CanUserResize="False"  Width="SizeToHeader">
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button x:ConnectionId='7' Margin="5 0 0 0"                     Tag="{Binding maloai}">
                                        <Image  Source="ms-appx:///Icons/edit.png" Width="16"/>
                                    </Button>
                                    <Button x:ConnectionId='8' Margin="5 0 0 0"                    Tag="{Binding maloai}">
                                        <Image  Source="ms-appx:///Icons/delete.png" Width="16"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Grid>
        <!--Pagination-->
        <Grid Grid.Row="3" Width="700">

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                <TextBlock FontWeight="Bold">Category per page</TextBlock>
                <Border Background= "{ThemeResource MyBlockBackgroundThemeBrush}" Margin="5,0,0,0">
                    <TextBlock Text="{Binding _rowsPerPage}"></TextBlock>
                </Border>

            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="10,0,0,0">
                <Button x:ConnectionId='3'             >Previous</Button>
                <Border Margin="10"  Background= "{ThemeResource MyBlockBackgroundThemeBrush}">
                    <TextBlock><Run Text="{Binding _currentPage}"/>/<Run Text="{Binding _totalPages}"/></TextBlock>
                </Border>
                <Button x:ConnectionId='4'              >Next</Button>
            </StackPanel>

        </Grid>
    </Grid>
</Page>

